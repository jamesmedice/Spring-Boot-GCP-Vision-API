spring:
    cloud:
        config:
            namespace: default
            enabled: true
            fail-fast: false 
            name: Cloud
            profile: dev, cloud, mysql
            label: master
        gcp:
            project-id: ${PROJECT_ID:qwiklabs-gcp-01-745d6fb7769b}
            credentials:
                location: classpath:key.json    