spring:
    cloud:
        config:
            namespace: default
            enabled: true
            fail-fast: false 
            name: Cloud
            profile: dev, cloud, mysql
            label: master
        gcp:
            project-id: ${PROJECT_ID:qwiklabs-gcp-04-4dcafd953ded}
            credentials:
                location: classpath:key.json    