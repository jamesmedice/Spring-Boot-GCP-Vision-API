spring:
    cloud:
        config:
            namespace: default
            enabled: true
            fail-fast: false 
            name: Cloud
            profile: dev, cloud, mysql
            label: master
        gcp:
            project-id: ${PROJECT_ID:qwiklabs-gcp-47982db821b8e864}
            credentials:
                location: classpath:key.json    