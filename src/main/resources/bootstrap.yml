spring:
    cloud:
        config:
            namespace: default
            enabled: true
            fail-fast: false 
            name: Cloud
            profile: dev, cloud, mysql
            label: master
        gcp:
            project-id: ${PROJECT_ID:qwiklabs-gcp-01-84f24d62813f}
            credentials:
                location: classpath:key.json    