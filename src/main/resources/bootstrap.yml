spring:
    cloud:
        config:
            namespace: default
            enabled: true
            fail-fast: false 
            name: Cloud
            profile: dev, cloud
            label: master
        gcp:
            project-id: ${PROJECT_ID:qwiklabs-gcp-01-8e343ec0963b}
            credentials:
                location: classpath:key.json    